# Tasks: A new company is picked up at once and says where it stands

1. Service tests first: due-work discovery, the scheduler's unchanged catalog, and the
   reported preparation state across queued, preparing and finished.
2. Add `due_tenants` and use it for the worker role in the sweep.
3. State the one-second default in the worker image.
4. Report `preparation` on the company setup receipt.
5. Read immediately in `followSetup`; render the three steps in the preparing screens.
6. Add the step strings in four languages; extend the browser script.
7. Run the focused suites, the web checks, the browser run and the full suite.
