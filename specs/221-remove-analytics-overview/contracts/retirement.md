# Retirement contract

GET /api/tenants/{tenant_id}/analytics and GET on its /contributors path are removed.
POST /analytics/query/contributors remains supported. Default and obsolete Overview URLs
open Explore; reports URLs preserve My reports. Home retains a simple analytics link.
