# Verification

Run `make spec-check`, `make lint`, `make test` and `make web-build`.
Open Analytics from Home and a legacy Overview URL: two tabs, Explore selected, no
overview GET requests. Open My reports and return to Explore; preserve company context.
