FFFFFFFFFFFFFFF                                                          [100%]
=================================== FAILURES ===================================
__________________ test_selection_retains_distinct_item_bases __________________

session = <sqlalchemy.orm.session.Session object at 0x10dd8b620>
business = Business(tenant=<reality.db.core.Tenant object at 0x10e07a030>, company=<reality.db.core.Party object at 0x10ed994c0>,...ed9a180>, item=<reality.db.core.Item object at 0x10ed9b680>, location=<reality.db.core.Location object at 0x10ed9b620>)
cost_owner = <reality.db.core.AppUser object at 0x10edc0d70>
monkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x10edc0bf0>

    def test_selection_retains_distinct_item_bases(
        session, business, cost_owner, monkeypatch
    ):
        tenant = business.tenant.id
        first = build(session, business, cost_owner)
        other = SimpleNamespace(**vars(business))
        other.item = core.create_item(session, tenant, "SECOND", "Second item")
        second = build(session, other, cost_owner)
        expected = sorted(
            [
                costing.inventory_cost_snapshot(
                    session, tenant, business.item.id, generation_id=first
                ),
                costing.inventory_cost_snapshot(
                    session, tenant, other.item.id, generation_id=second
                ),
            ],
            key=lambda row: row["generation_id"],
        )
        assert (
            expected[0]["context"]["knowledge_at"] != expected[1]["context"]["knowledge_at"]
        )
    
        def forbidden(*args, **kwargs):
            pytest.fail("Historical selection attempted replay or live cursor lookup")
    
        monkeypatch.setattr(
            "reality.services.inventory_generations.inventory_cost", forbidden
        )
        monkeypatch.setattr("reality.services.inventory_generations._sequence", forbidden)
>       result = costing.inventory_cost_snapshots(session, tenant, [second, first])
                 ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
E       AttributeError: module 'reality.services.costing' has no attribute 'inventory_cost_snapshots'. Did you mean: 'inventory_cost_snapshot'?

tests/test_inventory_snapshot_selection.py:53: AttributeError
____________ test_selection_foreign_missing_members_refuse_equally _____________

session = <sqlalchemy.orm.session.Session object at 0x10ee0cb30>
business = Business(tenant=<reality.db.core.Tenant object at 0x10b93f440>, company=<reality.db.core.Party object at 0x10f180ad0>,...f1807a0>, item=<reality.db.core.Item object at 0x10f182e40>, location=<reality.db.core.Location object at 0x10f180ec0>)
cost_owner = <reality.db.core.AppUser object at 0x10eed4710>

    def test_selection_foreign_missing_members_refuse_equally(
        session, business, cost_owner
    ):
        tenant = business.tenant.id
        generation = build(session, business, cost_owner)
        foreign = core.create_tenant(session, "Foreign selection")
        errors = []
        for selected_tenant, ids in (
            (foreign.id, [generation]),
            (foreign.id, ["absent"]),
            (tenant, [generation, "absent"]),
        ):
            with pytest.raises(core.NotFound) as error:
>               costing.inventory_cost_snapshots(session, selected_tenant, ids)
                ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
E               AttributeError: module 'reality.services.costing' has no attribute 'inventory_cost_snapshots'. Did you mean: 'inventory_cost_snapshot'?

tests/test_inventory_snapshot_selection.py:94: AttributeError
______________ test_selection_two_queries_without_flush_or_replay ______________

session = <sqlalchemy.orm.session.Session object at 0x10edde0c0>
business = Business(tenant=<reality.db.core.Tenant object at 0x10edde150>, company=<reality.db.core.Party object at 0x10f0bfb00>,...eeafe60>, item=<reality.db.core.Item object at 0x10eed4830>, location=<reality.db.core.Location object at 0x10ef64e30>)
cost_owner = <reality.db.core.AppUser object at 0x10eddd880>

    def test_selection_two_queries_without_flush_or_replay(session, business, cost_owner):
        tenant = business.tenant.id
        first = build(session, business, cost_owner)
        other = SimpleNamespace(**vars(business))
        other.item = core.create_item(session, tenant, "SECOND", "Second item")
        second = build(session, other, cost_owner)
        pending = Item(
            id=core.uid("itm"), tenant_id=tenant, sku="PENDING", name="Pending", unit="pcs"
        )
        session.add(pending)
        statements = []
    
        def watch(conn, cursor, statement, parameters, context, executemany):
            statements.append(statement.lower())
    
        event.listen(session.bind, "before_cursor_execute", watch)
        try:
            for ids in ([first], [first, second]):
                statements.clear()
>               costing.inventory_cost_snapshots(session, tenant, ids)
                ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
E               AttributeError: module 'reality.services.costing' has no attribute 'inventory_cost_snapshots'. Did you mean: 'inventory_cost_snapshot'?

tests/test_inventory_snapshot_selection.py:119: AttributeError
______________ test_selection_corrupt_member_refuses_whole_result ______________

session = <sqlalchemy.orm.session.Session object at 0x10f079c10>
business = Business(tenant=<reality.db.core.Tenant object at 0x10f098050>, company=<reality.db.core.Party object at 0x10bc2ed20>,...f1201d0>, item=<reality.db.core.Item object at 0x10f1232f0>, location=<reality.db.core.Location object at 0x10f2389e0>)
cost_owner = <reality.db.core.AppUser object at 0x10ee0c770>

    def test_selection_corrupt_member_refuses_whole_result(session, business, cost_owner):
        tenant = business.tenant.id
        generation = build(session, business, cost_owner)
        row = session.scalar(
            select(CostInventorySnapshot).where(
                CostInventorySnapshot.tenant_id == tenant,
                CostInventorySnapshot.generation_id == generation,
            )
        )
        row.acquisition_value += 1
        session.flush()
        with pytest.raises(core.InvalidOperation, match="integrity mismatch"):
>           costing.inventory_cost_snapshots(session, tenant, [generation])
            ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
E           AttributeError: module 'reality.services.costing' has no attribute 'inventory_cost_snapshots'. Did you mean: 'inventory_cost_snapshot'?

tests/test_inventory_snapshot_selection.py:145: AttributeError
__________ test_selection_invalid_shape_refuses_before_database[ids0] __________

ids = []

    @pytest.mark.parametrize(
        "ids",
        [
            [],
            (),
            "abc",
            {"abc"},
            [""],
            [" "],
            [1],
            [True],
            [None],
            ["one", "one"],
            [str(i) for i in range(101)],
        ],
    )
    def test_selection_invalid_shape_refuses_before_database(ids):
        class NoDatabase:
            def __getattr__(self, name):
                pytest.fail("Invalid selection accessed the database")
    
        with pytest.raises(core.InvalidOperation, match="selection"):
>           costing.inventory_cost_snapshots(NoDatabase(), "tenant", ids)
            ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
E           AttributeError: module 'reality.services.costing' has no attribute 'inventory_cost_snapshots'. Did you mean: 'inventory_cost_snapshot'?

tests/test_inventory_snapshot_selection.py:170: AttributeError
__________ test_selection_invalid_shape_refuses_before_database[ids1] __________

ids = ()

    @pytest.mark.parametrize(
        "ids",
        [
            [],
            (),
            "abc",
            {"abc"},
            [""],
            [" "],
            [1],
            [True],
            [None],
            ["one", "one"],
            [str(i) for i in range(101)],
        ],
    )
    def test_selection_invalid_shape_refuses_before_database(ids):
        class NoDatabase:
            def __getattr__(self, name):
                pytest.fail("Invalid selection accessed the database")
    
        with pytest.raises(core.InvalidOperation, match="selection"):
>           costing.inventory_cost_snapshots(NoDatabase(), "tenant", ids)
            ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
E           AttributeError: module 'reality.services.costing' has no attribute 'inventory_cost_snapshots'. Did you mean: 'inventory_cost_snapshot'?

tests/test_inventory_snapshot_selection.py:170: AttributeError
__________ test_selection_invalid_shape_refuses_before_database[abc] ___________

ids = 'abc'

    @pytest.mark.parametrize(
        "ids",
        [
            [],
            (),
            "abc",
            {"abc"},
            [""],
            [" "],
            [1],
            [True],
            [None],
            ["one", "one"],
            [str(i) for i in range(101)],
        ],
    )
    def test_selection_invalid_shape_refuses_before_database(ids):
        class NoDatabase:
            def __getattr__(self, name):
                pytest.fail("Invalid selection accessed the database")
    
        with pytest.raises(core.InvalidOperation, match="selection"):
>           costing.inventory_cost_snapshots(NoDatabase(), "tenant", ids)
            ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
E           AttributeError: module 'reality.services.costing' has no attribute 'inventory_cost_snapshots'. Did you mean: 'inventory_cost_snapshot'?

tests/test_inventory_snapshot_selection.py:170: AttributeError
__________ test_selection_invalid_shape_refuses_before_database[ids3] __________

ids = {'abc'}

    @pytest.mark.parametrize(
        "ids",
        [
            [],
            (),
            "abc",
            {"abc"},
            [""],
            [" "],
            [1],
            [True],
            [None],
            ["one", "one"],
            [str(i) for i in range(101)],
        ],
    )
    def test_selection_invalid_shape_refuses_before_database(ids):
        class NoDatabase:
            def __getattr__(self, name):
                pytest.fail("Invalid selection accessed the database")
    
        with pytest.raises(core.InvalidOperation, match="selection"):
>           costing.inventory_cost_snapshots(NoDatabase(), "tenant", ids)
            ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
E           AttributeError: module 'reality.services.costing' has no attribute 'inventory_cost_snapshots'. Did you mean: 'inventory_cost_snapshot'?

tests/test_inventory_snapshot_selection.py:170: AttributeError
__________ test_selection_invalid_shape_refuses_before_database[ids4] __________

ids = ['']

    @pytest.mark.parametrize(
        "ids",
        [
            [],
            (),
            "abc",
            {"abc"},
            [""],
            [" "],
            [1],
            [True],
            [None],
            ["one", "one"],
            [str(i) for i in range(101)],
        ],
    )
    def test_selection_invalid_shape_refuses_before_database(ids):
        class NoDatabase:
            def __getattr__(self, name):
                pytest.fail("Invalid selection accessed the database")
    
        with pytest.raises(core.InvalidOperation, match="selection"):
>           costing.inventory_cost_snapshots(NoDatabase(), "tenant", ids)
            ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
E           AttributeError: module 'reality.services.costing' has no attribute 'inventory_cost_snapshots'. Did you mean: 'inventory_cost_snapshot'?

tests/test_inventory_snapshot_selection.py:170: AttributeError
__________ test_selection_invalid_shape_refuses_before_database[ids5] __________

ids = [' ']

    @pytest.mark.parametrize(
        "ids",
        [
            [],
            (),
            "abc",
            {"abc"},
            [""],
            [" "],
            [1],
            [True],
            [None],
            ["one", "one"],
            [str(i) for i in range(101)],
        ],
    )
    def test_selection_invalid_shape_refuses_before_database(ids):
        class NoDatabase:
            def __getattr__(self, name):
                pytest.fail("Invalid selection accessed the database")
    
        with pytest.raises(core.InvalidOperation, match="selection"):
>           costing.inventory_cost_snapshots(NoDatabase(), "tenant", ids)
            ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
E           AttributeError: module 'reality.services.costing' has no attribute 'inventory_cost_snapshots'. Did you mean: 'inventory_cost_snapshot'?

tests/test_inventory_snapshot_selection.py:170: AttributeError
__________ test_selection_invalid_shape_refuses_before_database[ids6] __________

ids = [1]

    @pytest.mark.parametrize(
        "ids",
        [
            [],
            (),
            "abc",
            {"abc"},
            [""],
            [" "],
            [1],
            [True],
            [None],
            ["one", "one"],
            [str(i) for i in range(101)],
        ],
    )
    def test_selection_invalid_shape_refuses_before_database(ids):
        class NoDatabase:
            def __getattr__(self, name):
                pytest.fail("Invalid selection accessed the database")
    
        with pytest.raises(core.InvalidOperation, match="selection"):
>           costing.inventory_cost_snapshots(NoDatabase(), "tenant", ids)
            ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
E           AttributeError: module 'reality.services.costing' has no attribute 'inventory_cost_snapshots'. Did you mean: 'inventory_cost_snapshot'?

tests/test_inventory_snapshot_selection.py:170: AttributeError
__________ test_selection_invalid_shape_refuses_before_database[ids7] __________

ids = [True]

    @pytest.mark.parametrize(
        "ids",
        [
            [],
            (),
            "abc",
            {"abc"},
            [""],
            [" "],
            [1],
            [True],
            [None],
            ["one", "one"],
            [str(i) for i in range(101)],
        ],
    )
    def test_selection_invalid_shape_refuses_before_database(ids):
        class NoDatabase:
            def __getattr__(self, name):
                pytest.fail("Invalid selection accessed the database")
    
        with pytest.raises(core.InvalidOperation, match="selection"):
>           costing.inventory_cost_snapshots(NoDatabase(), "tenant", ids)
            ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
E           AttributeError: module 'reality.services.costing' has no attribute 'inventory_cost_snapshots'. Did you mean: 'inventory_cost_snapshot'?

tests/test_inventory_snapshot_selection.py:170: AttributeError
__________ test_selection_invalid_shape_refuses_before_database[ids8] __________

ids = [None]

    @pytest.mark.parametrize(
        "ids",
        [
            [],
            (),
            "abc",
            {"abc"},
            [""],
            [" "],
            [1],
            [True],
            [None],
            ["one", "one"],
            [str(i) for i in range(101)],
        ],
    )
    def test_selection_invalid_shape_refuses_before_database(ids):
        class NoDatabase:
            def __getattr__(self, name):
                pytest.fail("Invalid selection accessed the database")
    
        with pytest.raises(core.InvalidOperation, match="selection"):
>           costing.inventory_cost_snapshots(NoDatabase(), "tenant", ids)
            ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
E           AttributeError: module 'reality.services.costing' has no attribute 'inventory_cost_snapshots'. Did you mean: 'inventory_cost_snapshot'?

tests/test_inventory_snapshot_selection.py:170: AttributeError
__________ test_selection_invalid_shape_refuses_before_database[ids9] __________

ids = ['one', 'one']

    @pytest.mark.parametrize(
        "ids",
        [
            [],
            (),
            "abc",
            {"abc"},
            [""],
            [" "],
            [1],
            [True],
            [None],
            ["one", "one"],
            [str(i) for i in range(101)],
        ],
    )
    def test_selection_invalid_shape_refuses_before_database(ids):
        class NoDatabase:
            def __getattr__(self, name):
                pytest.fail("Invalid selection accessed the database")
    
        with pytest.raises(core.InvalidOperation, match="selection"):
>           costing.inventory_cost_snapshots(NoDatabase(), "tenant", ids)
            ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
E           AttributeError: module 'reality.services.costing' has no attribute 'inventory_cost_snapshots'. Did you mean: 'inventory_cost_snapshot'?

tests/test_inventory_snapshot_selection.py:170: AttributeError
_________ test_selection_invalid_shape_refuses_before_database[ids10] __________

ids = ['0', '1', '2', '3', '4', '5', ...]

    @pytest.mark.parametrize(
        "ids",
        [
            [],
            (),
            "abc",
            {"abc"},
            [""],
            [" "],
            [1],
            [True],
            [None],
            ["one", "one"],
            [str(i) for i in range(101)],
        ],
    )
    def test_selection_invalid_shape_refuses_before_database(ids):
        class NoDatabase:
            def __getattr__(self, name):
                pytest.fail("Invalid selection accessed the database")
    
        with pytest.raises(core.InvalidOperation, match="selection"):
>           costing.inventory_cost_snapshots(NoDatabase(), "tenant", ids)
            ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
E           AttributeError: module 'reality.services.costing' has no attribute 'inventory_cost_snapshots'. Did you mean: 'inventory_cost_snapshot'?

tests/test_inventory_snapshot_selection.py:170: AttributeError
=========================== short test summary info ============================
FAILED tests/test_inventory_snapshot_selection.py::test_selection_retains_distinct_item_bases
FAILED tests/test_inventory_snapshot_selection.py::test_selection_foreign_missing_members_refuse_equally
FAILED tests/test_inventory_snapshot_selection.py::test_selection_two_queries_without_flush_or_replay
FAILED tests/test_inventory_snapshot_selection.py::test_selection_corrupt_member_refuses_whole_result
FAILED tests/test_inventory_snapshot_selection.py::test_selection_invalid_shape_refuses_before_database[ids0]
FAILED tests/test_inventory_snapshot_selection.py::test_selection_invalid_shape_refuses_before_database[ids1]
FAILED tests/test_inventory_snapshot_selection.py::test_selection_invalid_shape_refuses_before_database[abc]
FAILED tests/test_inventory_snapshot_selection.py::test_selection_invalid_shape_refuses_before_database[ids3]
FAILED tests/test_inventory_snapshot_selection.py::test_selection_invalid_shape_refuses_before_database[ids4]
FAILED tests/test_inventory_snapshot_selection.py::test_selection_invalid_shape_refuses_before_database[ids5]
FAILED tests/test_inventory_snapshot_selection.py::test_selection_invalid_shape_refuses_before_database[ids6]
FAILED tests/test_inventory_snapshot_selection.py::test_selection_invalid_shape_refuses_before_database[ids7]
FAILED tests/test_inventory_snapshot_selection.py::test_selection_invalid_shape_refuses_before_database[ids8]
FAILED tests/test_inventory_snapshot_selection.py::test_selection_invalid_shape_refuses_before_database[ids9]
FAILED tests/test_inventory_snapshot_selection.py::test_selection_invalid_shape_refuses_before_database[ids10]
15 failed in 4.52s
