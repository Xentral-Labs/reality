# Catalog contract
Existing keys/queries remain accepted. Node responses add optional category (localized
label) and aliases (search strings); no request schema change. Dedicated subtype nodes
scope roots and positions. All public graph entrypoints use the same model. Catalog
observed values obey the identical tenant/type constraints as query execution.
