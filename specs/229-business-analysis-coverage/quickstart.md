# Validation
Run reporting graph and analysis-builder pytest files against isolated PostgreSQL;
run web test:contracts, build, i18n:audit, format:check. Run spec policy and docs catalog
generation/check. In Chrome open Data explorer, search supplier invoices and credits,
preview records, follow a line to its item, open in Analysis, inspect grouped areas.
Expected: named types only, currency preserved, old queries reopen, no writes required.
