# Data model

No data-model changes. Existing Tenant, Selection and ChatSession remain authoritative.
Transient UI state: navigation open/collapsed, chat open, description/action/profile
popover visibility. Existing tenant switches reset scoped chat. Shared activity readers remain tenant-scoped;
the Shell has no activity drawer state.
