# Research

Decision: retain existing source table and API, separate preparation cards above it. Source registration is not connection proof.
Decision: personal sessionStorage drafts, authenticated user + tenant key. No persisted business configuration or credential capture. Alternative new integration schema rejected for UI-only scope.
Decision: textual provider badges and category icons, not downloaded logos or external requests. Brand identity comes from full provider names; connector availability remains explicit.
Decision: native dialog and current localization system; no new component library.
