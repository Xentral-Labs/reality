# Data model

No changes. RealityGap owns evidence, recommendation, decision and implementation-proposal
entries; versioned rules retain existing source mapping and typed Fact definition. A draft is
local form state until confirmed prepare. Simulation is read-only; replay returns a cursor and
counts. Neither display totals nor manual notes become new source authority.
