# Design Decisions

Decision: compact shared rows with container-relative columns. Rationale: side chat reduces usable width independently of viewport width. Alternative: merely reduce padding, which retains the empty horizontal area. Keep icons as familiar anchors and full-row buttons for large targets. Existing browser fixture already covers all three queues and their read-only interactions.
