# Presentation Contract

WorkRow keeps its existing props, accessible name, aria-expanded and aria-controls. One row button opens one existing preview. Responsive columns depend on the parent list width; full data remains in the DOM. Search/filter callbacks and API inputs are unchanged.
