# Decisions
Reuse the existing company agent rather than the read-only lesson companion.
Reuse bounded trace JSON for reply-to-call references; a new schema is unnecessary.
Reject timestamp-range attribution because concurrent chats/manual calls overlap.
Handoff stays an explicit editable draft; no automatic send across a mode change.
No new technology choices or unresolved research requires a sub-agent.
