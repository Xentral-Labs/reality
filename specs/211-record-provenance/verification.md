# Verification

## Pre-implementation review
Scope approved in conversation on 2026-09-16, including the explicit decision to place the external base address on `SourceSystem`. Fourteen requirements map to acceptance scenarios, tests and implementation tasks with no unmapped task. The Constitution Check passes all eight principles pre-design. The one schema decision requiring product-scope approval (FR-007, `connector_code`) was approved on 2026-09-16 after the existing association defect was demonstrated: with the present catalog, an instance installed from `shopify_payments` satisfies the description-prefix test for both `shopify_payments` and `shopify`, so `connector_shells` already lists it under two connectors. The requirements checklist is complete at 8/8.

Initial failing proof, observed before the implementation it proves:
- `test_an_instance_belongs_to_exactly_one_connector_shell` and `test_each_installed_instance_is_listed_once_across_the_catalog` failed against the description-prefix inference, reporting the extra pair `('shopify', 'shopify_payments_de')`.
- The origin, addressing, inspection and contributing-system tests failed on the missing `reality.services.provenance` module, then on each missing behavior in turn.

## Post-design Constitution Check
PASS for all eight principles, re-reviewed against the implementation. No column was added to `SourceRecord` or to any operational table; the source record's textual system code remains the join key, so evidence does not depend on later configuration; the payload is rendered verbatim and bounded; every read and the one configuration write are tenant-scoped and go through shared services.

## Verification completed on 2026-09-16
- Targeted PostgreSQL suites: `tests/test_provenance.py` 35 passed and `tests/test_source_system_addressing_migration.py` 2 passed, covering batched origin resolution, the manual-origin fallback and its actor, the retained-code join, cross-tenant behavior, address validation and clearing, template resolution and encoding, the absent-template and absent-address states, superseded versions, the bounded payload, interpretation outcomes including the `not_recorded` label, contributing systems, and the connector-association regression. `tests/test_application_catalog.py` and `tests/tenant_isolation` 45 passed after classifying `set_source_system_base_url` and moving the pinned discovered-operation count from 486 to 487.
- Full PostgreSQL suite: 2,660 passed, 9 skipped, 0 failed in 606 s. An earlier full run of the same tree reported five failures (`test_demo_profile_history`, two `test_free_playground` parameterizations, `test_scheduled_worker`, `test_storyline_chat`); that run shared the machine with a Vite server and a Chromium browser session. All five passed when rerun together in isolation, and the repeat full run on a quiet machine was clean. The failing tracebacks were not captured, so the load explanation is inferred from the clean repeat rather than read from the output.
- `tests/test_telemetry.py` is excluded from both full runs and is red in this environment for an unrelated reason: the shared virtualenv predates the OpenTelemetry dependency added to `pyproject.toml` on main, so its five tests fail with `ModuleNotFoundError: No module named 'opentelemetry'` with and without this change. CI installs dependencies fresh.
- Ruff and the specification policy check pass. The coverage matrix names both new test files.
- Frontend: 208 contract tests pass, including the new `provenance-labels.test.mjs`; four-language audit passes at 1892/1892; production TypeScript/Vite build passes. The pre-existing bundle-size advisory is unchanged.
- Browser acceptance passed against local Vite with fixtures: origin stated on every row in all four languages with no empty statement, the hand-entered row saying so, link present for a configured system and absent for an unconfigured one, `target="_blank"` with `rel="noopener noreferrer"` and the target host in the title, a superseded version disclosed, the source inspection showing system, interpretation and the retained payload behind its disclosure, the contributing-system disclosure on a master-data record, and no horizontal page scroll at 390px. Every request the browser issued was a GET. Screenshots at `/private/tmp/reality-211-browser/` (local artifacts).

## Findings recorded rather than fixed
- The master-data detail panel in `MasterDataPage.tsx` is unreachable on `origin/main`: its `<section>` carries `className={record ? "hidden" : ...}` while its content renders only when `record` is set, so the existing "Original source" button there can never be seen. This change therefore renders origin in the row preview, which is the surface users actually reach, and leaves the dead panel untouched. It deserves its own fix.
- `make docs-catalog-check` cannot be verified in this environment: regenerating the catalog reference reformats all 19 generated files (20,945 insertions, 19,440 deletions) from a formatter-version difference alone. The identical diff appears with this change's catalog edits reverted, so it is pre-existing and no generated output is committed here.
- Address templates key on the vendor source type retained on the source record. Master data created with a source annotation records the target family (`party`) instead, so such records state their origin but resolve no link. This is specified behavior under FR-008 (nothing is guessed) and is covered by a test paired with a positive control.
- Declared `deep_links` cover `shopify`, `stripe` and `hubspot` only. Every other connector deliberately ships without a template until its address shape is confirmed against the vendor; the three declared shapes were written from general knowledge and were not verified against a live tenant.

## Change isolation
An isolated `211-record-provenance` worktree off `origin/main` carries only this feature. The unrelated uncommitted work in the user's main checkout was neither read into nor modified by this change.
