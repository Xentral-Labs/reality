# Verification
Use local PostgreSQL and the repository Python environment. Run targeted chat stream/provider/history/inventory tests first, then `make test`, `make lint`, `make spec-check`, `make web-build`, `make docs-generate` and confirm generated catalog output is stable. Run the delayed-provider browser proof: answer must be visible before done; final text must appear exactly once, including when metadata reload is delayed. Repeat the two original read questions and record first text/full response and provider cache/timing without changing model or confirming proposals. All implementation gates and actual limitations belong in verification.md.
