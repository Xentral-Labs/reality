# Implementation Plan: Unified tool catalog
## Technical Context
Python deployment metadata plus React/TypeScript presentation; no persistence changes. Read docs/WEB_SPEC.md, constitution and workflow. Existing application-reference response gains additive capability metadata. MCP catalog and server remain untouched.
## Constitution Check
I–VIII PASS: no business writes or derived authority; existing source/evidence links and confirmation retained; tenant boundary unchanged; no schema; tests precede implementation; no new dependencies.
## Design
`reality/tool_catalog.py` composes presentation entries from validated commands, discovery forms, views, projections and public MCP definitions. `config/tool_catalog.json` holds explicit topic/read equivalences and intent overrides; existing command coverage maps MCP without guessing. Equivalent reports merge by projection reference. Distinct forms retain their intent and linked command. Unmatched public tools remain visible. Validate references, stable IDs and total source coverage. Expose through runtime_application_catalog only (additive Web metadata), preserving load_application_catalog and every MCP definition.
`ToolCatalog.tsx` replaces old two-page presentation, using existing eligibility, form callbacks and report dialogs. Native disclosures show technical definitions and inputs. Technical-only entries identify their access rather than promising an unavailable Web action. Existing URLs remain; single header title Tools. MCP-capable entries can append a tenant-scoped prompt to the existing chat composer without sending it or replacing a draft; direct Web controls reuse current adapters. The existing open-chat event accepts a narrowly validated capability draft payload, separate from analytics handoff.
## Tests and rollout
Backend pure tests cover completeness, dedup, form variants, valid relationships and MCP snapshot compatibility. Frontend contract tests cover filtering and legacy section navigation. Browser checks cover both URLs, translated search, filters, disclosures, report/form access, member eligibility, tenant changes, keyboard and mobile. Run web-build, spec-check, lint, docs-catalog-check and focused backend catalog/MCP tests. No migration; rollback by reverting metadata/UI changes. Do not alter the original dirty checkout.
