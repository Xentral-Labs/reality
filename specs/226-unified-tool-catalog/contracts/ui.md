# UI and compatibility contract
Both inspector_view=commands and inspector_view=views select Tools and the same directory without tabs. Query/topic/purpose filters compose; visible count counts capabilities. Topic sections are expanded lists, detail disclosures are lazy. Existing direct forms honor discovery eligibility; reports use existing readers and explanation dialogs. MCP definition references remain exact and schema is displayed only on disclosure. No changes under reality/mcp, tools or services. Known report aliases share one entry; navigation links are marked as such.
