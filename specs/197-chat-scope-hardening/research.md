# Decisions
Use a shared instruction contract, not a keyword blocklist that would reject valid business questions. Preserve hard authorization outside the model. Reject invalid history roles instead of promoting or silently interpreting them. Live model behavior remains probabilistic.
