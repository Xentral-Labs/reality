# Research

Decision: explicit labeled presentation on the two integration tables.
Rationale: existing actionContent replaces children with a fallback ExternalLink and CSS constrains buttons to 24px. Both rendering and CSS need a scoped override.
Alternatives: changing icons alone leaves ambiguous meaning; changing every table would conflict with spec 164's preview-only rows. A menu adds unnecessary interaction for two actions. No unresolved research questions.
