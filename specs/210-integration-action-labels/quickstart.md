# Validation

Use the installed Playwright module/executable through PLAYWRIGHT_MODULE and PLAYWRIGHT_EXECUTABLE. Start the Web Vite server on port 5177 or set UNIFIED_BASE_URL.

Run `npm run test:sources-browser`, `npm run test:source-configuration-browser` and `npm run test:tables-browser` from apps/web. Sources fixtures intercept all APIs; no business database writes occur. Inspect `/private/tmp/reality-111-browser/de-dark-1440-records.png` and systems/mobile counterparts. Then run the required gates in plan.md.
