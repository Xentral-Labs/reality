# UI contract

RegisterTable accepts optional actionPresentation: icons (default) or labels. Labels preserve the provided localized control contents and existing event handlers. Callers allocate actionWidth for their labels. Only systems and records opt in.
