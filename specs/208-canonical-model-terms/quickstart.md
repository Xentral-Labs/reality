# Validation

Run from apps/web: `node --test scripts/model-terminology.test.mjs`, `npm run test:contracts`, `npm run i18n:audit`, `npm run build`.
Run `make spec-check` from the repository root.
In an authenticated company, switch among de/nl/es; check Home, sidebar and Facts type dropdown, search and record links. Model labels stay English, business partners/items/locations and controls stay localized. Switch to English and verify its existing copy.
