# Browser compatibility

Current /app destinations remain unchanged. Old orders/commitments, warehouse registers, finance registers, master data, integration/evidence, profile/company settings, chat and explorer bookmarks resolve to their corresponding retained workspace. Only documented read context is copied. Unknown routes show unavailable with an App link.

/playground and nested bookmarks show retirement with an App link. No old shell loads. Account returns accept only local supported app/retirement paths; arbitrary URLs are rejected. Pending users cannot bypass production approval. Backend Playground endpoints remain protected and unchanged.
