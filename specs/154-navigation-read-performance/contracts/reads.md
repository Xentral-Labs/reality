# Contracts

All HTTP and UI contracts remain unchanged. First-match reverse translation, original content and language switching are preserved. GET finance/payments retains bounded rows and complete filtered currency totals; its checkpoint alone refreshes after events. Explicit full refresh remains compatible.
