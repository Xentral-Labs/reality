# Data model

No schema or relationship change. Existing payments, postings, allocations and reversals remain authoritative. ProjectionRow/ProjectionCheckpoint are disposable existing caches. The browser reverse index contains only static translation catalog entries, never business values.
