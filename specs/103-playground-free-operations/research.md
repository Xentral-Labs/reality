# Research

Existing Playground order/reserve/movement inputs already select opaque targets.
The last-step coupling is UI-only. Reuse commitment-control with additive target
fields and paging rather than derive business state from bounded history.
Shopify import currently has intermediate commits and a separate interpretation
proposal; a sandbox allowlist shortcut is rejected. Bulk/import are later increments.
