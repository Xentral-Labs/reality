# Data Model: Repeating Form Groups

No persistent entities change. Presentation metadata adds a repeating-group definition with `key`, `label`, `children`, `defaults`, and `minimumRows`. Runtime form state contains an array of child-value objects and serializes directly to the existing request field.
