# Validation
Run `gmake spec-check web-build`. Start the web dev server and run the navigation browser scenario with PLAYWRIGHT_MODULE, PLAYWRIGHT_EXECUTABLE and UNIFIED_BASE_URL configured. Check four languages, both widths, old links and tab reload/back navigation.
