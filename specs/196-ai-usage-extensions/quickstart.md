# Validation

Use isolated PostgreSQL tests for the allowance service/API, then the Usage browser
fixture and make web-build. In local UI exhaust managed quota, open Usage, preview
and confirm +20, inspect actor/time/history, return to chat. Do not consume live
self extensions while running automated tests.
