# Data model

No persistent business model. One browser-local value `reality.navigation.collapsed` is `true` or `false`; only `true` initializes collapsed. Invalid or unavailable storage defaults to expanded.
