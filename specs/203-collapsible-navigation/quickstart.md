# Validation

Start Vite and set UNIFIED_BASE_URL, PLAYWRIGHT_MODULE and optionally PLAYWRIGHT_EXECUTABLE. Run `node apps/web/scripts/collapsible-navigation-browser.mjs`. Run frontend formatting, contract tests, localization audit, build and `python3 scripts/check_spec_policy.py`. Toggle on desktop, reload, open profile, navigate and resize to 390px.
