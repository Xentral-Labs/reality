# Validation
Run `gmake lint spec-check web-build`; run `PYTHONPATH=src ../../.venv/bin/pytest -q tests/test_operational_previews.py` from packages/reality-core. Review research.md against effective catalogs and inspect previews/master-data actions in de/nl/es. Existing model terminology and original-value tests must pass.
