# Data Model: Faster Quality Gates

No business or persistence entities change. The only transient input is a newline-delimited
set of repository paths; the classifier returns whether backend validation is required.

