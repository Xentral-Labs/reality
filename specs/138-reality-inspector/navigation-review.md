# FR-029 final review

Approved final names: Context, Facts, Rules, Actions; German Kontext, Fakten, Regeln, Aktionen. Facts is the broad recorded-business-information entry. The specific Fact family is Additional facts in the type filter; no domain identity, assertion authority or read/write semantics change. One primary paginated register replaces duplicate entry tabs, with a lazy technical disclosure retaining grouped raw fields. Legacy records URLs retain tenant/search/type filters.

Verification: navigation regressions failed before implementation, then all 57 isolated web tests and 59 integrated web tests passed. TypeScript/production build, Prettier, DE/NL/ES audit and spec policy passed. Native Chrome on port8080 confirmed four final sidebar labels, the Rules tab order before the final views relocation, All records plus Additional facts in the selector, technical grouped records on expansion, and Actions opening Event history before Action catalog. Web rebuilt and restarted with the root environment file. Backend content is identical to the already verified 335ef3e correction (1,964 tests passed, 9 skipped); no further backend changes or migrations.

The branch also carries the previously approved PR166 corrections because PR166 was merged into the intermediate 155 branch after PR165 had reached main. They must be included to retain graph and numeric-format behavior on main. Hosted CI remains dependent on the previously reported GitHub billing/spending-limit block; no hosted-success claim.

Final owner refinement: Calculated views now belongs to Facts, after All records; Rules retains Fact rules and Exception rules. Exact section-order and legacy-link tests updated (first failed, then passed), with 57 isolated / 59 integrated web tests green and production build/spec policy passing. Web rebuilt and restarted on port8080. A further native click check was stopped because the user changed the active Chrome page; the final tab ownership/order is covered by the executable navigation tests.
