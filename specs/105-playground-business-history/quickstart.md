# Verification — 2026-09-07
- FR-010: Shared Details control and rightmost Actions columns for five central
  registers; delivery operations retain callbacks and disabled conditions. Native
  delivery details use matching styling. Scoped wrapping removes the inherited
  220px first-column minimum; reviewed 1280px screenshot keeps Details visible.
  106 contracts, build, four-language audit, formatting, spec and diff checks pass.
  Browser verifies matching document/master Details styles and existing inspection,
  finance, guided and independent operations. Local web rebuilt successfully.
- FR-009: Documents tab reuses the sandbox-allowlisted evidence endpoint and canonical
  Inspector. 105 contracts pass after observed missing-component failure; build,
  formatting, four-language audit and spec policy pass. Isolated browser verifies
  page two, no-match/reset, document inspection and Back with no writes, plus prior
  guided/free/master journeys. Dark register screenshot reviewed. Web-only local
  deployment completed; no backend or schema changes. Search inherits existing
  number, type, opaque party ID and source-reference matching (not party-name search).
- FR-008: Shared borderless state presentation integrated into central registers.
  Browser no-match/reset regression and guided flow pass; 103 contracts, build,
  formatting, localization (1180 keys), spec and whitespace checks pass. Empty-state
  screenshot captured and reviewed. Web rebuilt and restarted locally.
- FR-007: Browser verifies matching master/history selector styles and catalog icon
  proximity; existing customer/supplier registers and guided flow pass. All 103
  frontend contracts, formatting, localization, production build and spec checks
  pass. Light screenshot reviewed; no service or business behavior changes.
- Shared timeline, Playground and master-data API suite: 101 passed.
- Frontend contracts: 103 passed. Localization: 1179 strings in all four languages.
- Production build, formatting, Ruff, spec policy and whitespace passed.
- Isolated browser: six-step guided story, Business history selection, canonical
  commitment→document traversal and Back, exact bottom-timeline highlight, search
  empty state, desktop light/dark screenshots and narrow layout inspected.
- Cursor/name-search regression uses 45-day-old events and verifies no overlap or
  confusion between event count and persisted movement count.
- No migration, mutation or auto-generated relationship. Groups reflect loaded
  matching source/correlation events; the complete order is not reconstructed.
  Existing narrow cockpit uses horizontal pane navigation; history itself stacks
  list/detail with bounded scroll areas at that width.
- No extension hooks configured. Reuse existing shared Inspector and timeline.
- FR-006: Central filters use 34px controls with wider, wrapping selects; operation
  forms remain unchanged. All 103 frontend contracts, browser height regression,
  production build, formatting, spec policy and whitespace checks passed. Updated
  light-theme screenshot inspected. Web container rebuilt and restarted locally.
# Search toolbar refinement — 2026-09-07
FR-013 business-first refinement: observed failing projection regression, then 48
business-event/master-data API tests passed. Includes business-area selection before
pagination, tenant boundaries, structured item context and counts independent of
event records. 119 frontend contracts, build, four-language audit (1228 keys), format,
Ruff and spec/diff checks passed. Full synthetic browser journey passed after updating
business-only inspector expectations; traversing a document retains its business
heading. Screenshots reviewed for business filters, metrics and context. API rebuilt
and healthy, web rebuilt. No schema or stored business-record changes. Business history
still exposes actual linked events only, never a fabricated complete order lifecycle.

FR-002/003 overview refinement: test-first contract then 118 contracts, build,
1221-key localization audit, format/spec/diff checks passed. Full synthetic browser
journey verifies four non-button metrics, both scope disclosures, Escape and retained
filter/search behavior. Reviewed history-data-overview.png and aligned metric heights.
No backend or stored data changes; explanation text remains accessible on demand.

FR-012 caption cleanup: regression failed before removing both inventory captions
and its generic action-effect footer. 117 frontend contracts, build and spec/diff
checks passed. Reviewed OpenWork, OpenItems, Documents and BusinessHistory: retained
functional filters and history completeness guidance; no other duplicate headings.

FR-011: regressions failed before shared fields/toggle implementation. Final evidence:
116 frontend contracts, production build, 1220-key four-language audit, formatting,
spec/diff checks and full synthetic Playground browser journey passed. Browser asserts
34px height, icon padding and accessible names in all four registers; settled-toggle
border, visible check, pressed state and unchanged item_status request semantics.
Existing search/no-match/reset and responsive/theme journeys remain green. Reviewed
search-toolbar.png; local web container rebuilt. No backend or business-state changes.
