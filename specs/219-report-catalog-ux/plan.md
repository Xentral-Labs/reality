# Plan
## Technical context
React/TypeScript frontend; existing ApplicationReference catalog and ProjectionDataDialog. No new dependencies or services.
## Constitution Check
All eight principles PASS before and after design: presentation only; immutable source/evidence/reality unaffected; no schema or authority changes; tenant-scoped readers reused; technical traceability retained; tests before implementation. No complexity exceptions.
## Design
Add reportCatalogEntries.ts for presentation metadata and grouping by exact data target, preserving authoritative live registers separately from stored snapshots. ReportCatalog.tsx supplies list, search and workspace filters using shared RegisterWorkbench. Extend ProjectionDataDialog with optional report title/summary and secondary details; default callers unchanged. Replace only the views branch in RealityInspectorPage. Translation copy stays in localization.tsx. No domain/service/tool changes required.
## Validation
Write grouping/filter tests first. Browser verifies catalog scanning, filtering/search, data open/close, technical details, price details-only, unknown entries, four languages, keyboard and mobile. Run spec-check and full web-build gate.
## Rollback and risks
Revert frontend commit; no migration. Exact-target grouping avoids conflating live and stored readings. Unknown entries retain original catalog labels/description. Price resolution must never invoke unsupported unparameterized read. Report details retain all catalog aliases and application links.

## Report context refinement
FR-006: adapt only ProjectionDataDialog.tsx. Keep the header outside a bounded scrolling body; put the existing catalog details in a responsive right column, ordered before data on mobile, expanded initially on desktop and collapsed initially on mobile (details-only reports stay expanded). Bound explanation height separately. Preserve existing freshness changes and all data readers. No new translations, schema, dependencies, domain/service/tool changes or calculations. Constitution Check: all eight principles PASS; user request authorizes this presentation refinement. First extend unified-inspector-browser.mjs with layout, disclosure and scrolling assertions; run REPORTS_ONLY in four languages, spec-check and web-build. Rollback is the layout patch only.

The user refinement removes repeated technical actions from the initial sidebar. RealityInspectorPage renders one canonical calculation/view explanation, followed by a single collapsed Technical details disclosure containing the existing catalog detail components. Preserve every original definition and link. Extend the existing browser regression to verify technical actions are initially hidden and become accessible on explicit expansion. No new translations or business logic; Constitution checks remain PASS.

## Designer-reviewed composition
Introduce ReportExplanation.tsx instead of reusing CatalogEntryDetails per alias. Generate deduplicated workspace hrefs through selectionUrl/navigationSelection. Add reportPresentation.ts for pure field ordering/labels and ReportDataTable.tsx for localized dates/booleans, exact decimal presentation and expandable lossless rows. Reuse RegisterTable with an explicit optional filterControl=false (default unchanged), a real final row-details column and locally neutral controls. ProjectionDataDialog retains freshness, stable header, reading states, limits and scrolling. No schema, services, dependencies or business calculations; all Constitution checks PASS. Tests: pure presentation fallback/order/identity preservation tests first, then browser canonical metadata/code/documentation counts, dispatch business columns, row-details preservation, mobile/focus/readers, complete spec/frontend gates. Rollback these presentation components; no migration.

## Shared directory refinement
Extract DirectoryBranch.tsx from ActionDirectory without changing behavior or selectors. ReportCatalog uses the same branches, indentation, disclosure rows, search toolbar and Expand all/Collapse all controls. groupReports assigns each exact report target to its first category with Company fallback; secondary categories remain in entry details and search. No extra folder hierarchy is invented. Add grouping tests before implementation and update the report browser for directory interaction, search expansion, report focus, mobile and four languages. Verify ActionDirectory through existing browser and frontend contracts. Constitution all PASS; no backend, schema, dependency or authority changes.
