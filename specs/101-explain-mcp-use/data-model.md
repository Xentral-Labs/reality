# Data Model: Explain MCP Use

No entities, fields, relationships, migrations, or stored state are added or changed. Documentation describes the existing tenant-scoped MCP endpoint, access tokens, tool allowlists, proposals, approvals, and verification records without redefining them.

