# Public Content Contract

## Landing page

The agent section ends with one compact MCP connection bridge rather than a capability grid. It identifies the immediate-read/approved-change boundary, describes HTTPS plus a restricted tenant token, and links to `/api-tools/connect-mcp`. Concrete jobs remain in the guide.

## Documentation

English and German guides have matching sections for prerequisites, endpoint and token setup, least privilege, generic configuration, first read, governed changes, troubleshooting, revocation, and current OAuth limitations. Both link to the canonical agent-capability guide and generated MCP catalog.
