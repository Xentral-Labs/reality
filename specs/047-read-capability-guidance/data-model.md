# Data Model: Read Capability Guidance

No persisted entity changes.

`ReadCapabilityGuidance` is versioned catalog metadata with public tool identity,
application-tool identity, semantic use boundaries, controlled data bases, freshness,
empty/refusal behavior, verification role and proof boundaries, unknown conditions,
next steps, and examples. It grants no access and stores no tenant data.

