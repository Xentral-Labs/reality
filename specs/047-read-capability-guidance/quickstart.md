# Quickstart: Read Capability Guidance

Describe each of the six reads through the application and MCP paths. Compare
`business_records_discover` with `order_explain`, and `inventory_read` with
`commitments_list`. Confirm every description contains a non-proof and unknown
condition. Run planted validator defects, existing proposal regressions, Docs tests,
then all repository gates.
