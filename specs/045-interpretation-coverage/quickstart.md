# Quickstart: Interpretation Coverage

Process a valid Shopify order and read its coverage; resolve all returned result IDs. Then exercise an unknown source, a failed attempt followed by successful retry, and a second-tenant lookup. Expect `interpreted`, `unsupported`, preserved attempt history, and not found respectively. Run targeted tests followed by the repository spec, lint, test, docs, and web gates.

