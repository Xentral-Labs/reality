# Data Model: Separate Public Site and Product Web App

No business entity, persistence field, relationship, state transition, Alembic revision,
or catalog entry is introduced.

The only configuration value is the public product-application origin used when the
static site constructs account links. It is deployment configuration, not business data.

Source → Evidence → Reality, tenant scoping, opaque identities, and every existing
schema contract remain unchanged.
