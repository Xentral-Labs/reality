# Read contracts

GET /attention and GET /finance/open-items retain their request/response shapes, filters, stable ordering, counts, complete filtered totals, authorization and evidence targets. The internal selective refresh advances only the financial checkpoint; explicit full refresh retains all-projection behavior. Exceptions still derives at the supplied observation instant and does not reuse inputs after the evaluation returns or fails.
