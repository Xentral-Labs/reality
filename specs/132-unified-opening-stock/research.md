# Research
Read-only plan research by order_research confirms opening_stock adds positive physical stock and does not require zero prior stock. Core validates stocked item, stock-capable location and identity, and already serializes movement writers. Inactive references are not rejected by core, so only their changed metadata makes a review stale. Quantity is Numeric(18,4); reject overprecision before a reviewed write. Reason is not stored on a normal movement, so no misleading reason form. Manual source-free movement already has attributable action/event evidence. Existing _opening_movement_evidence checks exact movement; wrapper must also validate event snapshot. Corrections append records and must not invalidate historical proof. Existing generic approval contract is retained outside the new state-bound entry.
