# Plan

Constitution PASS: no new authority/schema/business rules; opaque IDs and explicit links unchanged; read-only UI; tests first. Update flightRecorderGraph.ts to expose referenceColumns and place reference-only nodes in negative columns with two slots per lane/column. Observed event columns stay zero-based. FlightRecorder.tsx adds this width before the timeline, offsets all positions, and labels that area with the existing Earlier reference translation instead of dates. Existing viewport width-delta anchoring remains valid. Test dense references, occupancy, deduplication, promotion by older pages and explicit edge preservation. Run full web format/tests/i18n/build, inspect local rendering and rebuild web only. Roll back scoped frontend changes if needed.

Approved refinement: introduce a pure focus helper selecting incident edges and classifying direct neighbors by viewport bounds. Keep transitive connectedKeys for existing consumers/tests, but the timeline uses direct focus. Track scroll/resize bounds; render incident edges only when both endpoint cards intersect the viewport. Count hidden neighbors by direction and navigate via buttons. Reference keeps the same numbered, always-visible layout as the other lanes, with at most two earlier-reference slots per column. Preserve horizontal columns and event placement.

Owner correction: Reference uses the same always-visible numbered lane layout as all other lanes; remove collapse state/control. Inspector formatting restores WEB_SPEC and spec138 FR-028 through optional presentation parts on typed read values, preserving existing raw values. Shared browser rendering calls the existing locale formatters across modal/context/graph consumers. Parts are authored where values are composed, never parsed from business text. No schema, writes or query changes except the scoped parent document lookup needed to qualify a line amount's currency. Constitution: all PASS. Regression plan: preserved raw decimal/text/IDs; localized composite quantity/money and precise prices in all four locales; document-line currency and existing Inspector contracts. Rollback: remove additive metadata/rendering and rebuild.
