# Validation

Start apps/web Vite on port 5177. Set PLAYWRIGHT_MODULE to an installed Playwright index.mjs; run `node apps/web/scripts/unified-chat-composer-browser.mjs`.
Run `make web-build` and `make spec-check`.
Open fresh Chat: three questions; click one: focused editable draft; send: normal conversation. Check 390px and desktop, and supported languages.
