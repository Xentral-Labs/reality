# Quickstart

1. Enumerate public reads and confirm only `capability_describe` is exempt.
2. Describe `fulfillment_queue`, `exception_explain`, and `finance_balances`.
3. Confirm each response exposes a distinct proof and non-proof boundary.
4. Invoke `proposals_awaiting_approval` through MCP and the application dispatcher;
   compare results.
5. Plant an uncovered public read and confirm catalog validation fails.
