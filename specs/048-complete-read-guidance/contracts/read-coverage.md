# Read Coverage Contract

`required_guidance = public_mcp_reads - {"capability_describe"}`

Every required identity resolves to one guidance entry whose `application_tool` exists
and is non-mutating. Every entry declares data basis, freshness, limitations, empty and
refusal meaning, verification role/proofs/non-proofs, unknown conditions, next steps,
and positive/negative examples.
